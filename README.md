# AWS SupportCase Sample Code

Use AWS SDK to manage a [SupportCase](https://docs.aws.amazon.com/awssupport/latest/user/about-support-api.html) resource.

## Invocation

```sh
AWS_PROFILE=default go run main.go
```
